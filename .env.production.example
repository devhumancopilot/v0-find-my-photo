# =============================================================================
# PRODUCTION ENVIRONMENT VARIABLES
# =============================================================================
# Use these variables for your production deployment (Vercel, Netlify, etc.)
# IMPORTANT: Update all URLs from localhost to your production domain
# =============================================================================

# -----------------------------------------------------------------------------
# Supabase Configuration
# -----------------------------------------------------------------------------
# These can be the same as development OR use a separate production Supabase project
# Recommended: Use the same Supabase project for both dev and prod
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_production_supabase_anon_key
SUPABASE_SERVICE_ROLE_KEY=your_production_supabase_service_role_key
SUPABASE_JWT_SECRET=your_production_supabase_jwt_secret

# Production redirect URL (update with your production domain)
NEXT_PUBLIC_DEV_SUPABASE_REDIRECT_URL=https://yourdomain.com/dashboard

# -----------------------------------------------------------------------------
# n8n Webhook URLs
# -----------------------------------------------------------------------------
# Use the same n8n webhooks OR create separate production webhooks
# Recommended: Use the same webhooks (n8n can handle both dev and prod)
N8N_WEBHOOK_ALBUM_FINALIZED=https://your-n8n-instance.com/webhook/album-finalized
N8N_WEBHOOK_MANUAL_IMAGE_UPLOAD=https://your-n8n-instance.com/webhook/manual-upload
N8N_WEBHOOK_FIND_PHOTOS=https://your-n8n-instance.com/webhook/find-photos

# -----------------------------------------------------------------------------
# Google Photos Picker API Configuration
# -----------------------------------------------------------------------------
# IMPORTANT: Must update NEXT_PUBLIC_GOOGLE_REDIRECT_URI to production URL
# Go to: https://console.cloud.google.com/apis/credentials
# Edit your OAuth 2.0 Client ID and ADD production redirect URI:
# https://yourdomain.com/api/auth/google/callback

NEXT_PUBLIC_GOOGLE_CLIENT_ID=your_google_client_id_same_as_dev
GOOGLE_CLIENT_SECRET=your_google_client_secret_same_as_dev
NEXT_PUBLIC_GOOGLE_REDIRECT_URI=https://yourdomain.com/api/auth/google/callback

# -----------------------------------------------------------------------------
# Optional: PostgreSQL Direct Connection
# -----------------------------------------------------------------------------
# Usually not needed if using Supabase
# POSTGRES_URL=your_production_postgres_connection_string
# POSTGRES_PRISMA_URL=your_production_postgres_prisma_url
# POSTGRES_URL_NON_POOLING=your_production_postgres_non_pooling_url
# POSTGRES_HOST=your_production_postgres_host
# POSTGRES_USER=your_production_postgres_user
# POSTGRES_PASSWORD=your_production_postgres_password
# POSTGRES_DATABASE=your_production_postgres_database
